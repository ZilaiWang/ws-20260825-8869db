data_root: ""           # 数据集根目录（必填）
annotation_path: ""       # 标注文件路径
train_images: ""          # 训练图像目录
val_images: ""            # 验证图像目录
test_images: ""           # 测试图像目录
class_mapping: {}         # 数据集 category_id → 内部标签映射（TBD）
split_manifest: ""        # 训练/验证划分 manifest 路径
read_only: true           # 原始数据只读
