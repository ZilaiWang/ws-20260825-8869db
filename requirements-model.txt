# Install the CUDA build of torch/torchvision from pytorch.org first.
# Do not let pip silently select a CPU-only build on the training server.
torch>=2.1
torchvision>=0.16
scipy>=1.10
numpy>=1.24
PyYAML>=6.0
Pillow>=10.0
tqdm>=4.65
