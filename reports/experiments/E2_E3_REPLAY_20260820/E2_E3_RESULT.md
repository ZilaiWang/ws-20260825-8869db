# E2 + E3: Y5 直接重放实验(DCR²-YOLO 第一批, 2026-08-20)

> 服务器不可达(GPU 实例停止), 先完成纯本地可全量做的 replay 实验。
> 口径: Y5 三折 OOF 全量候选 65,301, 官方细类匹配。

## E2: aircraft 同细类 NMS@0.5 直接重放(0.001 全量口径)

| 指标 | Y5 基线 | Y5+NMS(aircraft@0.5) | Δ |
|---|---|---|---|
| TP | 20,391 | 20,390 | **-1** |
| FP | 44,910 | 38,286 | **-6,624** |
| FN | 542 | 543 | +1 |
| Recall | 0.9741 | 0.9741 | 0 |
| FDR | 0.6877 | 0.6525 | **-3.5pp** |

**结论**: NMS 组件在 Y5 上删 6,624 个重复 FP(有效), 但原始分数排序下
误伤 1 个 TP → **直接 NMS 不达标(准入: 任一 TP 损失即失败)**。
这验证了总纲判断: NMS 必须放在 **细类重排(post-rerank)之后**, 用重排后的
分数排序才安全(否则高分重复框压掉真 TP)。E2 完整版需 E1(crop 重排)前置,
待服务器恢复后跑。

## E3: Soft-Risk v0 有界残差重排(三折 cross-fit 逻辑回归)

- 特征: score / log(short_edge) / 宽高比 / 同图粗类密度 / 粗类 one-hot;
- 标签: **位置匹配**(该框与任一 GT 官方 IoU 匹配, 类别无关), 正样本 53.1%;
- P(loc) AUC = **0.9503**(位置匹配概率预测极好);
- 重排: z = logit(s) + clip(beta*logit(P(loc)), -c, c), 再 sigmoid。

### 阈值曲线对比(beta=0.5, clip=3.0)

| t | 基线 R/F | SoftRisk R/F | ΔR | ΔF |
|---|---|---|---|---|
| 0.01 | 0.9541/0.3607 | 0.9567/0.3272 | **+0.25pp** | **-3.35pp** |
| 0.05 | 0.9335/0.2034 | 0.9379/0.2029 | **+0.44pp** | -0.06pp |
| 0.1 | 0.9212/0.1567 | 0.9285/0.1662 | +0.74pp | +0.95pp |
| 0.2 | 0.9063/0.1169 | 0.9160/0.1345 | +0.97pp | +1.76pp |

### beta 扫描

| beta/clip | t=0.01 ΔR/ΔF | t=0.05 ΔR/ΔF |
|---|---|---|
| 0.5 / 3.0 | +0.25pp / **-3.35pp** | +0.44pp / -0.06pp |
| 0.7 / 2.0 | +0.32pp / -2.89pp | +0.60pp / +0.58pp |

### E3 结论

1. **机制验证成功**: 位置匹配概率 AUC 0.95, 低阈值区间(t≤0.05)Recall/FDR
   双改善——"低分真阳被上调"确实可学;
2. **局限**: 高阈值(t≥0.1)Recall 提升被 FDR 上涨抵消——P(loc) 高置信的框
   里混入 FP_CLS/FP_DUP(位置对但类错/重复), 简单单调重排无法区分;
3. **下一步(v1)**: ①标签改为"细类正确匹配"或加 fine-margin 特征(E4/E5 后)
   ②加重复框特征(同图高 IoU 多框计数)压 DUP ③与 E4 细类修正组合后,
   高阈值 FDR 问题应缓解——这正是 DCR² 解耦的意图(风险头管"在不在",
   FGR 管"是哪类");
4. E3 v0 作为 **Safe 版本组件** 可用: t=0.01 工作点 Recall 0.9567/FDR 0.327
   相对基线明显改善, 叠加 E2 的 aircraft NMS 后 FDR 还能再降。

## 产物

- scripts/soft_risk_v0.py(可复用, 换任何 predictions 文件即跑)
- /tmp/E3-softrisk-v0.json(beta=0.5 全阈值表)
- E2 重放逻辑已内嵌验证(脚本化见 scripts/ 后续 E2 正式版)

## E3 v1 追加:细类正确标签 + 重复框特征

- 标签改为"细类正确匹配"(同细类 IoU), 正样本率 37.2%; 加重复框特征
  (同图同细类 IoU>0.5 框数, mean 1.51 / max 30);
- P(fine) AUC = 0.9419; **FDR 全阈值大幅下降**(风险头变保守, 下调
  FP_CLS/重复框), 代价 Recall 略降。

| beta/clip | t=0.01 ΔR/ΔF | t=0.05 ΔR/ΔF | t=0.1 ΔR/ΔF |
|---|---|---|---|
| 0.5 / 3.0 | -0.82pp / **-8.12pp** | -0.85pp / -3.33pp | -0.61pp / -1.76pp |
| 0.3 / 2.0 | -0.48pp / **-5.54pp** | -0.54pp / -2.40pp | -0.46pp / -1.19pp |

### v0 vs v1 对比(设计洞察)

| | v0(位置标签) | v1(细类标签) |
|---|---|---|
| 学什么 | "该框对应对应一个真实目标" | "该框细类也正确" |
| 行为 | 上调低分真阳(保 Recall) | 下调 FP_CLS/重复(压 FDR) |
| 最佳区间 | t≤0.05 | t≥0.05 |
| 组合价值 | + Recall | - FDR |

**结论**: 两个头分别解决"在不在"与"对不对", 正对应 DCR² 的 RCR(风险)与
FGR(细类)解耦。最终 Safe 链 = Y5 + E3-v0(t≤0.05 区间) + E3-v1 叠加 +
E2-post-rerank-NMS(等 E1 crop 重排) —— 每层只动一类错误。
